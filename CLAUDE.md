# CLAUDE.md — agents-kit

Этот репозиторий — **механизм кита**, а не проект под китом: базы знаний здесь нет, и правила рабочего контура к самому киту не применяются.

Что кит делает, как ставится и как связывается с базой — [README.md](README.md). Инварианты, которые хук подаёт в каждую сессию под китом, — [reference/invariants.md](reference/invariants.md). Ни то, ни другое здесь не пересказывается: здесь только то, что нужно, когда правишь механизм.

## Что менять нельзя и почему

Решения, дорогие для переоткрытия. Причин в коде нет: он помнит выбранный вариант и не помнит, чем плохи остальные. Поэтому рядом с каждым стоит и то, что из него следует при правке.

- **Адрес базы — локальный git config копии, а не реестр маршрутов.** Реестр референса был машинно-зависимым файлом, требовал строки на каждую рабочую копию и дублировал список копий, живший в базе. git config не попадает в коммит, не виден в рабочем дереве и наследуется каждым `git worktree`. Отсюда запрет: машинно-зависимых путей в файлах кита не бывает — путь, попавший сюда, уехал бы к тому, кто поставит кит следующим.
- **Сторон связи две, и пишет их не человек, а одна команда.** Односторонний указатель врёт молча: каталог, скопированный вместе с `.git`, наследует его и начинает писать знание в чужую базу, пока путь существует и всё выглядит рабочим. Руками это две записи в разных местах, а забытая вторая даёт ровно то состояние, которое хук блокирует.
- **Ключ сверки — только абсолютный путь.** Опознание по `git remote` отвергнуто: у скопированного каталога тот же origin, то есть remote пропускает тот самый случай, ради которого сверка и заведена. Worktree покрывается через `git rev-parse --git-common-dir`, приводящий его к пути основной копии.
- **Раскладка базы расслоена по восстановимости, а не по темам.** Правило «дописывай, а не переписывай» держалось бы на дисциплине агента, а стирание причины выбора необратимо. Отсюда требование к правке: механика, перечитывающая код, пишет только в снимок и права на остальную базу не получает. Что где лежит и по каким правилам — [reference/base-layout.md](reference/base-layout.md).
- **Инварианты подаёт хук, а не user-level `CLAUDE.md`** — так они приходят только в сессию под китом. Отсюда требование к хуку: где кита нет, туда ни строки контекста, а любая внутренняя ошибка гасится и выходит нулём. Сломанный хук не должен ломать сессию.
- **Резолв не раздваивается.** Состояние связи целиком — и нормализация путей, и порядок проверок — определяет `link-state.ps1`; хук и `link.ps1` только переводят готовое состояние в свой вывод. Разъедься нормализация, сверка начнёт врать в обе стороны; разъедься порядок, новое состояние учтут в одном вызывающем и забудут в другом.

## Как править

- **Правка действует со следующей сессии только при установке каталогом автозагрузки.** Чем это отличается от установки маркетплейсом — README, «Установка».
- **После правки — самотест:** `pwsh -NoProfile -File scripts\selftest.ps1`. Он заводит во временной папке репозиторий, базу, копию каталога вместе с `.git` и worktree, прогоняет каждый случай через настоящий хук и убирает за собой. Красный даёт код возврата 1; `-KeepTemp` оставляет каталоги для разбора.
- **У каждого файла одна ответственность, и она видна из имени.** У скрипта это вопрос, на который он отвечает; у текста — читатель и момент: кто его открывает и когда. Резать текст по темам нельзя: тогда каждому читателю нужен кусок из каждого файла, и тема расползается по всем сразу — так и заводится дублирование. Имя вида `common` или `utils` заводить нельзя ни там, ни там: по нему не понять ни что внутри, ни что туда класть, поэтому такой файл собирает всё подряд.
- **Механизм нейтрален к проекту.** Всё, что верно только для одного проекта, живёт в его базе, а не здесь. Правка кита, начинающаяся со слов «а вот на этом проекте надо…», почти всегда должна быть правкой базы.

## Как вести эти документы

- **Этот файл держится под 200 строк, и каждая строка проходит тест «убери её — начнёт ли сессия ошибаться?».** Он грузится в каждую сессию, открытую здесь, а длинный файл съедает контекст и хуже исполняется. Не начнёт ошибаться — вырезать; выводится из кода или из дерева каталогов — вырезать. Растёт — дробить не импортами `@файл` (они грузятся при старте так же), а по адресам: нужное только при работе с определёнными файлами — в `.claude/rules/` с `paths:`, пошаговая процедура — в скилл, обязательное всегда — в хук. Проверить обрезку можно `/doctor`: он предлагает вырезать выводимое из кода и оставляет грабли, причины решений и отступления от умолчаний.
- **Заметка человеку — в HTML-комментарий, и только в этом файле.** Из `CLAUDE.md` блочные комментарии вырезаются до подачи в контекст, а `reference/invariants.md` хук читает как файл и отдаёт целиком: спрятанное там не спрячется, а попадёт в каждую сессию под китом.
- **У каждого утверждения один адрес.** Повтор допустим только как ссылка, а не как пересказ: два описания одного разъедутся, и никакая проверка этого не поймает. Правило действует и на эти файлы — `README.md`, `CLAUDE.md`, `reference/invariants.md`.
